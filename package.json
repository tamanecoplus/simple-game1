{
  "name": "simple-game1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 5173"
  },
  "devDependencies": {}
}
